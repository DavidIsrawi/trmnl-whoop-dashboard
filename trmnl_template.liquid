<div class="view whoop-dashboard">
  <div class="header">
    <div class="brand">
      <span class="logo">WHOOP</span>
      <span class="status-indicator"></span>
    </div>
    <div class="time-box">
      <div class="date">{{ last_updated | date: "%A, %b %d" }}</div>
      <div class="sync">SYNC: {{ last_updated | date: "%H:%M" }}</div>
    </div>
  </div>

  <div class="main-grid">
    <!-- Recovery Card -->
    <div class="card recovery-card">
      <div class="card-label">RECOVERY</div>
      <div class="card-main">
        <div class="large-value">{{ recovery_score }}<span class="pct">%</span></div>
        <div class="history-bars">
          {% for score in recent_recoveries %}
            <div class="bar-wrapper">
              <div class="bar" style="height: {{ score }}%"></div>
            </div>
          {% endfor %}
        </div>
      </div>
      <div class="card-footer-metrics">
        <div class="mini-metric">
          <span class="m-label">HRV</span>
          <span class="m-value">{{ hrv | round: 0 }}<span class="u">ms</span></span>
        </div>
        <div class="mini-metric">
          <span class="m-label">RHR</span>
          <span class="m-value">{{ resting_heart_rate }}<span class="u">bpm</span></span>
        </div>
      </div>
    </div>

    <!-- Strain Card -->
    <div class="card strain-card">
      <div class="card-label">STRAIN</div>
      <div class="card-main">
        <div class="large-value">{{ strain | default: '0.0' }}</div>
        <div class="history-bars">
          {% for s in recent_strains %}
            <div class="bar-wrapper">
              <div class="bar" style="height: {{ s | times: 4.76 }}%"></div>
            </div>
          {% endfor %}
        </div>
      </div>
      <div class="card-footer-metrics">
        <div class="mini-metric">
          <span class="m-label">WEEKLY AVG</span>
          <span class="m-value">{{ weekly_strain_avg | default: '0.0' }}</span>
        </div>
        <div class="mini-metric">
          <span class="m-label">CALORIES</span>
          <span class="m-value">{{ kilojoules | divided_by: 4.184 | round: 0 | default: '0' }}</span>
        </div>
      </div>
    </div>

    <!-- Sleep Card -->
    <div class="card sleep-card">
      <div class="card-label">SLEEP</div>
      <div class="card-main">
        <div class="large-value">{{ sleep_performance }}<span class="pct">%</span></div>
        <div class="sleep-quality-bar">
          <div class="progress-fill" style="width: {{ sleep_performance }}%"></div>
        </div>
      </div>
      <div class="card-footer-metrics">
        <div class="mini-metric">
          <span class="m-label">EFFICIENCY</span>
          <span class="m-value">{{ sleep_efficiency | default: '--' }}%</span>
        </div>
        <div class="mini-metric">
          <span class="m-label">RESPIRATORY</span>
          <span class="m-value">{{ respiratory_rate | round: 1 }}<span class="u">rpm</span></span>
        </div>
      </div>
    </div>
  </div>

  <div class="bottom-bar">
    <div class="vital-item">
      <span class="v-label">SpO2</span>
      <span class="v-value">{{ spo2 | default: '--' }}%</span>
    </div>
    <div class="v-divider"></div>
    <div class="vital-item">
      <span class="v-label">SKIN TEMP</span>
      <span class="v-value">{% if skin_temp %}{{ skin_temp | round: 1 }}°C{% else %}--{% endif %}</span>
    </div>
    <div class="v-divider"></div>
    <div class="vital-item">
      <span class="v-label">VO2 MAX</span>
      <span class="v-value">{{ vo2_max | default: '--' }}</span>
    </div>
  </div>
</div>

<style>
  * { box-sizing: border-box; }
  body { margin: 0; padding: 0; }
  .whoop-dashboard {
    height: 100%;
    display: flex;
    flex-direction: column;
    background: #fff;
    color: #000;
  }

  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    border-bottom: 2px solid #000;
  }

  .logo {
    font-size: 28px;
    font-weight: 900;
    letter-spacing: -1.5px;
  }

  .time-box { text-align: right; }
  .date { font-size: 16px; font-weight: 700; }
  .sync { font-size: 9px; color: #666; letter-spacing: 0.5px; }

  .main-grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    flex-grow: 1;
  }

  .card {
    padding: 15px;
    border-right: 1px solid #ddd;
    display: flex;
    flex-direction: column;
  }

  .card:last-child { border-right: none; }

  .card-label {
    font-size: 12px;
    font-weight: 800;
    letter-spacing: 1.5px;
    border-bottom: 1px solid #000;
    padding-bottom: 4px;
    margin-bottom: 10px;
  }

  .card-main {
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  .large-value {
    font-size: 56px;
    font-weight: 900;
    line-height: 1;
    margin-bottom: 10px;
  }

  .pct { font-size: 20px; font-weight: 600; margin-left: 2px; }

  .history-bars {
    display: flex;
    align-items: flex-end;
    gap: 3px;
    height: 30px;
    width: 80%;
    margin-top: 5px;
  }

  .bar-wrapper {
    flex: 1;
    height: 100%;
    background: #f0f0f0;
    display: flex;
    align-items: flex-end;
  }

  .bar {
    width: 100%;
    background: #000;
  }

  .sleep-quality-bar {
    width: 80%;
    height: 6px;
    background: #f0f0f0;
    border-radius: 3px;
    overflow: hidden;
  }

  .progress-fill {
    height: 100%;
    background: #000;
  }

  .card-footer-metrics {
    margin-top: 15px;
    display: flex;
    flex-direction: column;
    gap: 5px;
  }

  .mini-metric {
    display: flex;
    justify-content: space-between;
    font-size: 11px;
  }

  .m-label { color: #555; font-weight: 600; }
  .m-value { font-weight: 800; }
  .u { font-size: 8px; margin-left: 1px; }

  .bottom-bar {
    display: flex;
    justify-content: space-around;
    align-items: center;
    background: #000;
    color: #fff;
    padding: 12px 0;
  }

  .vital-item {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .v-label { font-size: 9px; font-weight: 700; margin-bottom: 3px; opacity: 0.7; }
  .v-value { font-size: 15px; font-weight: 800; }
  .v-divider { width: 1px; height: 18px; background: rgba(255,255,255,0.3); }
</style>
